import * as assert from "assert";
import * as vscode from "vscode";

suite("Bug Detector Extension Test Suite", () => {
  vscode.window.showInformationMessage("Starting all tests...");

  // write the tests
});
